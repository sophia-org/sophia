# One task per line, a blank line between tasks: keep it that way so the file reads as a list. A row is `(priority) date text ... @context id:tNNN order:NNN [details](note)`; closed rows move to done-YYYY-MM.md.

(B) 2026-09-20 Replace the remaining wall-clock waits outside the private-input controls with progress-bounded ones and generated failure text: x11_socket/tests/routing.rs (12), tests/support/m3_acceptance_c.rs (9), tests/connection_wait.rs (6), the desktop comparison workload.rs (6), the xterm command (6) and the shell launcher (5), plus the fixed-step class such as state_only_frozen_release_retains_original_request_then_applies_once_after_exact_thaw, which pumps a fixed count and cannot say whether it waited long enough. +development @development id:t131 order:000.00022 [details](docs/notes/investigations/yo5l2jui-private-input-controls-fail-on-a-wall-clock-deadline-under-load.md)

(B) 2026-09-20 Find why rejected_profile_admission_fails_before_negotiated saw a second event after Failed under load: it asserts try_event() is Err once the failure event is read, and one run in six under 64 spinners found another event there; establish what the worker emits after a rejected admission and whether the control or the worker is wrong. +development @development id:t132 order:000.00023 [details](docs/notes/investigations/yo5l2jui-private-input-controls-fail-on-a-wall-clock-deadline-under-load.md)

(B) 2026-09-20 Review the connection admission and launch origin tracking security gaps (evaluate process re-parenting, SCM_RIGHTS, PID namespaces, and DoS), and design a kernel-level pidfd-based verification pipeline to optimize and secure client admission. +development @development id:t133 order:000.00024 [details](docs/notes/investigations/1pv291te-namespace-and-client-admission-security-gaps.md), [plan](docs/notes/plans/esnqxpqw-pidfd-and-namespace-admission-optimizations.md)

(B) 2026-09-20 Review host security confinement policies, and evaluate Rust-native unprivileged sandboxing alternatives (unshare, libcontainer) to replace the external Bubblewrap dependency for Sophia supervisor roles. +development @development id:t135 order:000.00026 [details](docs/notes/investigations/q2pdd8gn-unprivileged-sandboxing-library-alternatives-to-bubblewrap.md)

(B) 2026-09-20 Serve several client groups from one session process on several listeners, in the existing synchronous frontend and with admission still deciding: XServerFrontend holds a table of (listener, namespace context) and the loop's already-nonblocking accept iterates it from the same thread; XServerFrontendAdmissionRequest carries the accepting listener and LiveXAdmissionPolicy returns that group's ClientAdmissionContext after the existing credential check, refusing an unknown listener rather than defaulting; the live session creates one namespace per configured group. No async runtime -- the same thread accepting on every listener is what keeps t138's idle-window reclaim honest, and a control must pin that a departure on one listener is not reclaimed while another has a live client. The portal-granted transfer across two sockets is proven for CLIPBOARD and PRIMARY both (clipboard_frontend.rs, parametrised over the selection), so paste between sandboxes has its witness before this ships. Carries the half of t141 that needs a confined listener to exist before it can be done: the launcher mounts exactly one group directory at the sandbox's standard X11 socket path, and the exit-3 proof that a client launched into a group cannot reach the trusted path -- cannot reach it, not is denied at it. Tier 2 of the plan. Once listeners exist, --admit-xtest=<group> nominates one group's namespace for XTEST; the policy is already keyed on the namespace and needs no change. +development @development id:t142 order:000.00033  [plan](docs/notes/plans/ooy00zjd-socket-directory-and-frontend-multiplexer-architecture.md)

(B) 2026-09-20 Let the virtual input device click, move vertically and scroll, so the physical acceptance rows can be driven unattended: tools/probes/uinput_text_injector.py registers BTN_LEFT, REL_X and REL_Y as capabilities -- BTN_LEFT precisely so libinput recognises the device -- but emits only EV_KEY and REL_X, so it can type, send the logout and recovery chords and shake horizontally, and cannot click, drag or scroll. Filling in the emitters beside shake, with a wheel axis registered, unblocks t004, t009, t011, t060 and t062, which need nothing else; t007, t012, t019 and t077 need no new input at all. Driving a session is not accepting one, so decide per row whether a recorded unattended run satisfies its accept/observe/revalidate. Carries the policy t094 left here: the injector already sends the reserved chord (KEY_LEFTCTRL, KEY_LEFTALT, KEY_BACKSPACE, keycode 14 being t139's own constant), and /dev/uinput is gated by the input group, whose members can already read every keystroke -- so refusing it would break the recovery rehearsal without raising the bar. Recommended: allowed and marked, with an emergency chord from a virtual device a valid rehearsal and never a valid acceptance, requiring virtual=false as the two-keyboard verifier does. Recorded as a recommendation for Mason to overrule, not a settled decision. +development @development id:t145 order:000.00036 [details](docs/notes/investigations/wq3n8fkz-which-physical-acceptance-rows-a-virtual-device-could-drive.md)

(B) 2026-09-19 Measure scroll delivery under a high-rate wheel or touchpad the way the shake measures motion, then decide whether to sum axis deltas per frame: PointerAxis is still routed one delivery per packet, the shape that cost the pointer frames, at roughly a tenth the rate. It cannot take the pointer's repair -- each packet carries a delta, so latest-wins would lose scroll distance and summing changes what a client sees. Unmeasured, so measure before changing. +development @development id:t121 order:000.0008 [details](docs/notes/investigations/4cv3tvhj-which-other-input-paths-carry-the-per-event-and-blocking-commit-faults.md)

(B) 2026-09-19 Route touch input, which no event kind exists for today: the libinput poller counts touch devices and nothing delivers a touch event, so a touchscreen is inert under Sophia. Wanted for a Lenovo X13. Build it with per-contact latest-wins coalescing released on the frame boundary from the start, since touch motion has the pointer's shape and would otherwise need the same repair later. +development @development id:t122 order:000.0009 [details](docs/notes/investigations/4cv3tvhj-which-other-input-paths-carry-the-per-event-and-blocking-commit-faults.md)

(B) 2026-09-19 Evaluate the thirty core X11 opcodes the authority still does not decode, each of which becomes BadRequest and so decides whether a client survives meeting it: KillClient is what a window manager uses when a client ignores WM_DELETE_WINDOW, the keyboard and pointer control requests would have to be answered by policy since Engine owns input, and the host access-control family describes a mechanism Sophia deliberately does not have; decide per request whether to serve, answer with a protocol error, or leave undecoded, and record the reasoning. +parallel @planning id:t125 order:125 [details](docs/notes/concepts/3wbcpd5c-core-x11-protocol-coverage.md)

(A) 2026-09-08 Implement a universal device-negotiation solution equivalent in purpose to Wayland DMA-BUF feedback, without executable-name allowlists or special user recipes. +critical @development id:t069 order:000.50 [details](docs/notes/plans/6tewvlbh-universal-device-negotiation-across-sophia-clients.md)

(A) 2026-09-13 Admit explicit direct GPU access on stock Linux and close compositor backing charges without claiming a hard VRAM quota. +critical +lom @development id:t097 order:000.801 peer:lom/t003 [details](docs/notes/plans/1m3z9q0j-lom-and-sophia-portable-gpu-shell-critical-path.md#t097)

(A) 2026-09-13 Complete anchored content popout presentation and consumed outside dismissal with parent-loss recovery. +critical +lom @development id:t099 order:000.803 depends:t098 peer:lom/t005 [details](docs/notes/plans/1m3z9q0j-lom-and-sophia-portable-gpu-shell-critical-path.md#t099)

(A) 2026-09-13 Repair native repaint deferral and startup topology, then complete content epoch, reconnect and retirement recovery through production owners. +critical +lom @development id:t100 order:000.804 depends:t097,t098,t099 peer:lom/t006 [details](docs/notes/plans/1m3z9q0j-lom-and-sophia-portable-gpu-shell-critical-path.md#t100)

(A) 2026-09-13 Verify one combined content/descriptor shell, exact Lom package and profile, and independent Narthex rollback. +critical +lom @development id:t101 order:000.805 depends:t097,t098,t099,t100 peer:lom/t007,t020 [details](docs/notes/plans/1m3z9q0j-lom-and-sophia-portable-gpu-shell-critical-path.md#t101)

(A) 2026-09-12 Accept Lom's globally numbered per-output refresh-safe workspace clicks/shortcuts, flash-free native panel, popouts and descriptor controls on every output with clean shutdown using exact installed evidence and a measured daily workload. +critical +lom @physical id:t081 order:000.85 depends:t101 peer:lom/t008,t009,t010 [details](docs/notes/plans/1m3z9q0j-lom-and-sophia-portable-gpu-shell-critical-path.md#t081)

(A) Complete the physical tab acceptance for frame-tree/Notion and i3/split-tree: empty/nested groups, hidden-member activation, shell recovery, title changes, fullscreen, and floating occlusion. +critical +cp14-3 @development id:t018 order:018 [details](docs/notes/plans/queue-06-4-exercise-real-development-workflows.md#t018)

(A) Review workflow evidence, recovery, clean teardown, bounded warmed resources/no steady-state allocation growth, and relevant refresh-relative latency evidence. +critical +cp14-3 @development id:t020 order:020 [details](docs/notes/plans/queue-07-5-close-milestone-14.md#t020)

(A) Record the concise result with zk milestone, link it from the milestone map in docs/notes/indexes/milestones.md, and update current product statements only after the development-session target is… +critical +cp14-3 @development id:t021 order:021 [details](docs/notes/plans/queue-07-5-close-milestone-14.md#t021)

(B) Accept namespace-scoped QueryPointer menu placement and drag behavior in the installed session. +parallel @physical id:t060 order:060 [details](docs/notes/plans/queue-11-parallel-production-readiness.md#t060)

Stamp geometry, interaction, pointer-handoff, and WM-policy work queued across an output-topology transition; revalidate it against typed authority epochs. +candidate @planning id:t031 order:031 [details](docs/notes/plans/queue-13-authority-and-lifecycle-hardening.md#t031)

Bound the page-flip callback read without losing retirement, then close the broker swallow points, unbounded input delivery, route-lease send, and config-bound publication silent drops. +candidate @planning id:t032 order:032 [details](docs/notes/plans/queue-13-authority-and-lifecycle-hardening.md#t032)

Decide whether blind spatial/output roles require Bubblewrap protection by default and define fail-closed behavior on hosts without bwrap. +candidate @planning id:t033 order:033 [details](docs/notes/plans/queue-13-authority-and-lifecycle-hardening.md#t033)

Specify secure lock/takeover admission, output coverage, fail-closed provider loss and trusted unlock through the input epoch barrier; no lock-screen UI project. +candidate @planning id:t034 order:034 [details](docs/notes/plans/queue-13-authority-and-lifecycle-hardening.md#t034)

Before admitting a content prototype, characterize Quickshell's retained popup-movement failure with an isolated display backend. +candidate @planning id:t035 order:035 [details](docs/notes/plans/queue-14-native-wm-and-shell-product.md#t035)

Optional short installed control smoke: discover, invoke a safe registered action, restart WM, and observe continued input/rendering. +candidate @planning id:t036 order:036 [details](docs/notes/plans/queue-14-native-wm-and-shell-product.md#t036)

Verify owner recovery and control-endpoint settlement before exposing scripted reload-profile; shortcut-driven reload acceptance remains in t076. +candidate @planning id:t037 order:037 [details](docs/notes/plans/queue-14-native-wm-and-shell-product.md#t037)

Harden the implemented issuer-scoped action checks and reservation/work-area coordination only against a named remaining lifecycle gap; preserve existing offline conformance and distinguish it… +candidate @planning id:t038 order:038 [details](docs/notes/plans/queue-14-native-wm-and-shell-product.md#t038)

Stabilize the minimum sophia_shell_v1 lifecycle only after CP-15.1 and CP-15.2; require signed installed Narthex evidence and preserve metadata separation from the blind WM. +candidate @planning id:t039 order:039 [details](docs/notes/plans/queue-14-native-wm-and-shell-product.md#t039)

Add bounded target-resolved move, resize, drag, and scrolling interactions. +candidate @planning id:t040 order:040 [details](docs/notes/plans/queue-14-native-wm-and-shell-product.md#t040)

(B) Implement and accept child-app origin placement and Triad-derived floating/dialog regressions. +parallel @development id:t041 order:041 [details](docs/notes/plans/queue-14-native-wm-and-shell-product.md#t041)

Specify the remaining redacted window/layout/focus feed and scoped actions for native clients; reuse existing descriptors and workspace indicators. +candidate @planning id:t043 order:043 [details](docs/notes/plans/queue-14-native-wm-and-shell-product.md#t043)

Publish docs/building-on-sophia.md as a public web page. +candidate @planning id:t044 order:044 [details](docs/notes/plans/queue-15-developer-outreach.md#t044)

Promote a confined daily-driver group only after Kitty and Firefox pass their grant and recovery gates. +candidate @planning id:t045 order:045 [details](docs/notes/plans/queue-16-portals-and-confined-applications.md#t045)

Close named portal/frontend/provider gaps for INCR, Xdnd, URI/file launch, prompts, notifications and capture/FD handoff; prove scoped grants with minimal clients, not a desktop UI suite. +candidate @planning id:t046 order:046 [details](docs/notes/plans/queue-16-portals-and-confined-applications.md#t046)

Before broadening the shell schema with effects, model capability admission, bounded parameters, supersession, Engine-clock cancellation, provider absence/failure, deterministic fallback, and… +candidate @planning id:t047 order:047 [details](docs/notes/plans/queue-17-compositor-graphics-and-effects.md#t047)

Implement one protocol-neutral Engine effect registry and private build-linked provider seam. +candidate @planning id:t048 order:048 [details](docs/notes/plans/queue-17-compositor-graphics-and-effects.md#t048)

Settle remaining display-list vocabulary from a driving client: generic target regions, desktop background, and only measured additions beyond client-rasterized textures. +candidate @planning id:t049 order:049 [details](docs/notes/plans/queue-17-compositor-graphics-and-effects.md#t049)

Retain only measured rendering follow-ups (cross-drawable CopyArea done 2026-09-24): bounded raster storage, upscale filtering, linear-light blend/opacity, mirror remode, presented-extent raster demand, CPU GBM… +candidate @planning id:t050 order:050 [details](docs/notes/plans/queue-17-compositor-graphics-and-effects.md#t050)

Keep XLibre+xmonad only as a direct external desktop-comparison reference; it never runs as Sophia policy and never connects to a Sophia endpoint. +candidate @planning id:t051 order:051 [details](docs/notes/plans/queue-18-compatibility-and-diagnostics.md#t051)

Retain physical glxgears, pc105 keyboard, exhaustive X11 reservation, Chromium fixture, and human-visible QEMU/RFB proofs only when they cover a promoted product change. +candidate @planning id:t052 order:052 [details](docs/notes/plans/queue-18-compatibility-and-diagnostics.md#t052)

Complete and verify the 36-row Kitty/Firefox/resize/launch-burst matrix against Sophia, XLibre+xmonad, and niri only when the user selects a stable candidate or a named performance investigation. +deferred +cp14-2 @planning id:t053 order:053 [details](docs/notes/plans/queue-20-cp-14-2-same-hardware-comparison.md#t053)

XLibre provider integration until a measured native-X gap justifies its authority and maintenance cost. +deferred @planning id:t054 order:054 [details](docs/notes/plans/queue-21-other-deferred-work.md#t054)

Any new application protocol or compatibility frontend without a specification amendment backed by named product evidence. +deferred @planning id:t055 order:055 [details](docs/notes/plans/queue-21-other-deferred-work.md#t055)

VRR until physical hardware reports vrr_capable=1. +deferred @planning id:t056 order:056 [details](docs/notes/plans/queue-21-other-deferred-work.md#t056)

Runtime effect plug-ins or a sandboxed effect host until the private build-linked provider proves a need and a safe lifecycle. +deferred @planning id:t058 order:058 [details](docs/notes/plans/queue-21-other-deferred-work.md#t058)

(B) Diagnose and repair blank or black Thunar menus with mapped-popup pixel evidence and installed acceptance. +parallel @development id:t061 order:061 [details](docs/notes/investigations/ce2b55uy-blank-thunar-menus-and-frozen-brave-need-separate-pixel-and-delivery-evidence.md)

(B) Accept explicit pointer-grab promotion and continued Brave interaction after the click-lease repair. +parallel @physical id:t062 order:062 [details](docs/notes/investigations/744uylx4-explicit-pointer-grabs-must-replace-their-own-click-lease.md)

2026-09-07 Restore application-selected cursor shapes, including the hand pointer when hovering over hyperlinks. +candidate @development id:t067 order:067 [details](docs/notes/investigations/lv26isrv-application-cursor-shapes-do-not-follow-hyperlink-hover.md)

(B) Implement optional Hagia focus-follows-mouse with default-off behavior and committed presented-target routing +parallel +policy @development id:t078 order:078 [details](docs/notes/investigations/nsu4a0n2-optional-pointer-focus-follows-presented-targets-through-committed-policy.md)

(B) 2026-09-12 Accept deployed uninstrumented pinentry 0.2.0 c9c1eda after t089 containment: verify live Dialog XID hints, floating placement, UTF-8 and the action/consecutive-dialog matrix; deployment and offline smoke passed, native acceptance pending. +parallel @development id:t082 order:082 [details](docs/notes/investigations/iux6ctsy-pinentry-submission-stalls-before-gui-exit-and-input-recovery-remains-blocked.md#pending-pinentry-work)

Measure CPU content costs before admitting shared-memory or DMA-BUF image transport with immutable acceptance and final release. +candidate +lom @planning id:t102 order:102 peer:lom/t016 [details](docs/notes/plans/1m3z9q0j-lom-and-sophia-portable-gpu-shell-critical-path.md#t102)

Evaluate an optional renderer-independent GPU bridge only against a named isolation or performance need and enforceable limits. +candidate +lom @planning id:t103 order:103 peer:lom/t019 [details](docs/notes/plans/1m3z9q0j-lom-and-sophia-portable-gpu-shell-critical-path.md#t103)

(A) 2026-09-17 Reconcile the implemented r7/r8 bar/launcher/dock admission, grants and language-neutral compatibility against the complete native component contract. +critical +native-components @development id:t104 order:104 [details](docs/notes/plans/ptil1ejw-modular-native-shell-components-and-independent-launcher-critical-path.md#t104)

(A) 2026-09-17 Complete remaining supervision, routing, revocation and legacy-compatibility evidence for implemented independent bar/launcher/dock admission. +critical +native-components @development id:t105 order:105 depends:t104 [details](docs/notes/plans/ptil1ejw-modular-native-shell-components-and-independent-launcher-critical-path.md#t105)

2026-09-17 Close remaining component placement, reservation, focus and presented-input lifecycle gaps through existing production owners. +candidate +native-components @planning id:t106 order:106 depends:t105,t099,t100 [details](docs/notes/plans/ptil1ejw-modular-native-shell-components-and-independent-launcher-critical-path.md#t106)

2026-09-17 Complete joined multi-component conformance, isolation, fairness and resource-reclamation gates using existing independent reference clients. +candidate +native-components @planning id:t107 order:107 depends:t106 [details](docs/notes/plans/ptil1ejw-modular-native-shell-components-and-independent-launcher-critical-path.md#t107)

2026-09-17 Accept the independent Lom/Bemenu/Provlita baseline on an exact installed release; verify failure isolation, performance, focus, two outputs and clean child/component teardown. +candidate +native-components @planning id:t108 order:108 depends:t107,t081 [details](docs/notes/plans/ptil1ejw-modular-native-shell-components-and-independent-launcher-critical-path.md#t108)

2026-09-18 Specify bounded admission and presentation for additional non-catalog native providers without impersonating bar/launcher/dock roles. +candidate +native-protocol @planning id:t109 order:109 depends:t104,t105,t106 [details](docs/notes/plans/1sxw3fyj-native-desktop-protocol-gaps-after-the-three-component-baseline.md#t109)

2026-09-18 Specify scoped idle observation and inhibition with bounded subscriptions, revocation and no raw input disclosure. +candidate +native-protocol @planning id:t110 order:110 [details](docs/notes/plans/1sxw3fyj-native-desktop-protocol-gaps-after-the-three-component-baseline.md#t110)

2026-09-18 Specify general native text and input-method semantics beyond launcher input, preserving exact presented focus and bounded disclosure. +candidate +native-protocol @planning id:t111 order:111 depends:t106 [details](docs/notes/plans/1sxw3fyj-native-desktop-protocol-gaps-after-the-three-component-baseline.md#t111)

2026-09-18 Specify opt-in native accessibility semantics and scoped actions with redaction, revocation and minimal independent probes. +candidate +native-protocol @planning id:t112 order:112 [details](docs/notes/plans/1sxw3fyj-native-desktop-protocol-gaps-after-the-three-component-baseline.md#t112)

2026-09-18 Specify confined access to existing desktop services for status and authorized effects; no unrestricted bus grant or settings UI project. +candidate +native-protocol @planning id:t113 order:113 [details](docs/notes/plans/1sxw3fyj-native-desktop-protocol-gaps-after-the-three-component-baseline.md#t113)

2026-09-19 Reconcile the WM layout, the frontend geometry and the Engine committed geometry after a layout timeout, and give an armed visual epoch a deadline of its own: expire_pending never rewrites layout.layers, epochs 80 and 118 stayed armed indefinitely in the 2026-09-19 session, and observe_authority_batch corrects layer geometry from the authority only for client-positioned surfaces. The compositor seam that made this visible is fixed; the divergence is not. +candidate @planning id:t127 order:127 [details](docs/notes/investigations/drutdyov-two-production-paths-framed-one-window-in-two-places.md)

2026-09-22 Repair the absolute paths the sophia-stack to sophia rename left behind: the dock-mode sibling guard in run_current_lom_panel_gate_tty4.sh compares against a checkout that no longer exists and refuses every dock run, the xtask dock_launcher fixture mirrors the old name, the t082 probe bundle names the old tree, and four documents tell a reader to cd somewhere absent; derive each from the checkout and keep the guard refusing a mismatched source. +candidate @development id:t151 order:151 [details](docs/notes/investigations/4p56e67s-the-checkout-rename-left-absolute-paths-that-break-a-gate-and-a-guard.md)

2026-09-22 Find what a second connection pair inherits from a departed first on the same instance: on a warm instance a later round takes focus and receives injected keys without admitting its own window as a surface, which a fresh instance refuses, so establish whether that is benign retained scene state or a window staying focusable after its owner has gone. +candidate @development id:t152 order:152 [details](docs/notes/investigations/gtdfc22g-a-warm-instance-lets-a-later-round-take-focus-without-admitting-its-own-surface.md)

2026-09-22 Decide whether the QEMU verifier's kernel page-flip coverage check is too strict for the software-GPU guest or is catching a real gap: two runs of identical healthy code produced timestamps=1 and timestamps=0 and the second failed verify_qemu_session_evidence.sh while the harness completed cleanly; re-run on unchanged code, read the distribution, and only then loosen or keep the assertion. +candidate @development id:t153 order:153 [details](docs/notes/investigations/3g6vzokc-the-qemu-verifiers-page-flip-coverage-check-can-fail-a-sound-run.md)

(B) 2026-09-20 Fix the timing race in a_key_through_the_service_before_any_pointer_observation_carries_the_prepared_position, which reads the delivery before the private service has flushed it: measured by the obligations lane at 2 in 40 on 76b5b920 and 1 in 40 three commits later, so it predates both lanes' work and gives every full run of the sophia-x-authority lib suite about a one in twenty chance of a spurious red. A harness race rather than a product defect until shown otherwise; wait on the flush, not on time. +important @development id:t148 order:000.00056

(B) 2026-09-23 Log per-event detail for routed pointer and XTEST input in the --verbose session log: each injected or physical pointer event with its global position, the Engine hit-test result (surface and local position, or a miss and the fallback taken) and the surface it was finally delivered to. The t124 Hagia run could only be read by adding temporary eprintln probes to the XTEST resolver; a verbose session should answer "where did this click go" on its own, without a rebuild, and the lines must stay out of retained evidence (debug level, positions only, no content). +important @development id:t157 order:157

(B) 2026-09-23 Reconcile the offered policy catalog and compiled-default shortcuts with admitted session operations. +important @development id:t159 order:159 [details](docs/notes/investigations/sp5qem3i-the-policy-catalog-must-admit-only-available-session-actions.md)

(B) 2026-09-23 Bring tools/fixtures/qemu_emergency_recovery_pass.log up to the schema its verifier now demands: 5729028c made verify_qemu_emergency_recovery_evidence.sh require the poller's schema=4 readiness line, the fixture still carries schema=2, and tools/check_atomic_scanout_verifiers.sh has failed at that expect_pass ever since ("missing physical input readiness"), masking every check after it. Take the new fixture from a real emergency-recovery guest run, not by hand. Found while adding the xtest-selection verifier's fixtures on 2026-09-23. +important @development id:t160 order:160

2026-09-23 Accelerate XRender compositing on the GPU, keeping the CPU raster as its oracle: core X drawing stays on the authority's CPU buffers because the protocol defines its pixels exactly and clients read them back, but Render's Composite, trapezoids and glyphs are the one heavy X drawing path where GPU work pays. Promote only with a measured driver (a client whose Render traffic is CPU-bound), a readback policy for GetImage and CopyArea on accelerated drawables, and x11bench's Render tests and XTS's Render cases passing on both paths, with the CPU path the reference and the fallback when no render device is present. +candidate @planning id:t177 order:177 [details](docs/notes/investigations/4ky4jyb9-x11bench-as-a-pixel-oracle-what-an-independent-drawing-suite-says-about-the-software-rasterizer.md)

(B) 2026-09-24 Make an abandoned render worker the output's failure, not the session's, and replace it: after t186 a render that never returns within ten seconds quarantines the facade and the failure still ends the session through the Present cohort (production_visual_runtime/native.rs, 'Present output cohort failed'), and the worker core (one thread per DRM device) is never replaced. Degrade that output alone, keep the others presenting, and respawn the core with a fresh EGL context under the live scanout, with the previous session's MissingSource(DmaBuf) ending reviewed in the same seam. Red is a fixture where one output's worker never returns while another keeps presenting. Scoped 2026-09-25, no source change: (1) after the ten-second abandon the facade quarantines and answers one Failed(WorkerStalled) poll, which the exporter reports Degraded and the native tick (production_visual_runtime/native.rs run_native_pending_output) turns into ScanoutExportFailed and so the session's retirement failure; every later submit answers WorkerPending, so an output whose worker was abandoned would otherwise look pending forever with the last frame kept on scanout; (2) the worker registry (exporter/worker/lifecycle.rs reserve) refuses a successor for a device whose previous worker has not finished, and an abandoned worker stuck in a GPU call never finishes, so a replacement needs a reservation that admits a successor beside an abandoned worker and forgets the old thread; (3) on a single-output desktop, the operator's, degrading the output without replacing the worker leaves a frozen screen with live clients, worse than the display manager's restart after the exit, so the degrade and the replacement land together, the exit kept as the last resort when the successor fails too. @development id:t191 order:191 [details](docs/notes/investigations/h833kgfy-one-hard-stall-of-the-rendered-scanout-export-worker-ends-the-session.md)

(C) 2026-09-24 Capture and settle the prepared-runner turn counts under load: x11_socket::routing_tests::a_prepared_runner_owns_state_before_exposing_its_real_producer (private_runner.rs:115) failed once in a full 'cargo test -p sophia-x-authority --lib' run beside other suites and passed three times alone; it asserts the exact starts, terminal steps, cleanup starts and enqueues of one service turn after one submitted press, with no wait, so either a turn's accounting depends on how far the producer's threads got before the turn (an assertion to widen or a fixture to sequence) or the count is wrong under contention (a bug). Reproduced 2026-09-24 with four copies of the routing tests running at once (eleven of twelve runs failed): a_key_through_the_service read the delivery cell's answer before it was written (7 of 12; now waited for), c_exact_origin read the other origin's store before its refusal's release (5 of 12; now waited for), and once each: d_service_exit 'assertion failed: step.charged' (m3_acceptance_lifecycle.rs:545), obligations_from_two_invocations_stay_distinct and a_stalled_private_reader 'each frontend numbered its transactions from one: the numbers collide, TransactionId(5)' (private_service_egress.rs:706, private_stalled_reader.rs:331), an_unwind_inside_the_private_service (private_service_egress.rs:112) and private_service.rs:374, the store-untouched shape again. The prepared-runner turn count did not recur. Settle the remaining five the same way, or find the counter they share. Found 2026-09-25, twenty-four loaded runs (four copies of the routing tests at once, six rounds): the counter is the service budget's charged time, two milliseconds per sixteen-millisecond interval charged in wall-clock, which one slow step under load empties for the next, so the next step yields TimeExhausted and a test that expects a charged step fails (d_service_exit 'assertion failed: step.charged' twice in twelve, a_terminal_step_whose_watch_refuses 'an admitted visit reports a step, not a yield' once); the lifecycle acceptance's step_charged and the runner test now retry a budget yield after its retry_after, bounded at three seconds, as production does on its next tick, and d_service_exit did not recur in the twelve runs after. Seen once each and still open: losing_the_command_channel and an_ordinary_stop_keeps_the_unsent_raster_envelope 'the transport still holds the worker's item' (private_service_egress.rs:378, twice across the hunts), obligations_from_two_invocations 'the numbers collide, TransactionId(6)' (a second frontend continuing the first's count, so the rounds share state under load), b_shared_hold Saturated (the route queue full under load, m3_acceptance_keyboard.rs:296), an_unwind_after_a_worker_started 'the launch scope did not return within the harness bound', d_service_exit's waited_for on admission_pause_pending, and a_recipient_disconnect Runner(Admission(NotAdmitted)) (m3_acceptance_recipient.rs:74). A third hunt of eight loaded runs with both retries in place passed seven, the one failing run losing b_shared_hold and obligations_from_two_invocations again and withheld_generic_recipient_custody_keeps_failed_output_and_control_credit once; neither budget shape recurred. @development id:t194 order:194

2026-09-24 Run the XTS Xlib11 event section through the adapter on the host and on Xvnc, repair what only the host fails, and declare the scenario: 195 purposes, Xvnc passes 121; the first host run passed 47 with XTEST admitted, WarpPointer moving nothing but QueryPointer's answer, InputOnly windows exposed and an unmapped save-set window left unmapped (all three repaired on the branch); t197, t198, t210 and t221 closed; the remaining families are t199, t211 and t220; the scenario is declared as xts_expected_events.json (123 passed, 72 declared, every declared row reasoned with its task) and runs under the gate with --xts-admit-xtest=yes; each seam that lands re-declares it. @development id:t196 order:196 [details](docs/notes/investigations/1qfs8k41-the-xts-event-section-on-the-host-and-on-xvnc.md)

2026-09-24 Report visibility from occlusion and crossing and keymap events from hierarchy changes: VisibilityNotify is Unobscured on map and never changes when a sibling covers or uncovers the window (XTS VisibilityNotify 2-3, 7-9); unmapping, mapping or reparenting a window under the pointer generates no EnterNotify or LeaveNotify (EnterNotify 1, 3; LeaveNotify 1), and no KeymapNotify followed an EnterNotify or FocusIn selected with KeymapState (KeymapNotify 1-3; done 2026-09-24 on both paths, the input writer's EnterNotify and the FocusIn of the focus records and of the protocol routing pass, carrying QueryKeymap's bitmap; the suite's KeymapNotify 1 binary segfaults on its own 'Missing %s event' report whenever an EnterNotify is the last event it reads, so on a quiet machine the purpose passes and is declared so; beside a full test suite it reads 'No events received' on four of its five warps, the t229 race). Done 2026-09-25 for pointer moves within a client's own windows and out to the root: the input writer generates the protocol's crossings from the pointer window's path (leaves up to the common ancestor, then enters down, with Ancestor, Virtual, Inferior, Nonlinear and NonlinearVirtual details, the child toward the pointer and coordinates in each window), a pointer injection over no toplevel goes to the last toplevel's client so it sees the pointer leave, and a KeymapNotify follows each written EnterNotify (EnterNotify 4, 7-9, 13 and LeaveNotify 4, 5, 8-10, 15 pass). The same day: a window destroyed under the pointer leaves it in the root for the next crossing, a peer selecting only crossings on the owner's window is told of the motion so its writer generates them, and the crossing's focus flag follows the focus projection (EnterNotify 3 and KeymapNotify 3 pass). The crossings from a hierarchy change under the pointer landed 2026-09-25: after a map, unmap, destroy, reparent, configure, circulate or subwindow request the connection routes the pointer's position again on its own thread, to the surface now under it, as a motion of no distance that every writer turns into the crossings it owes and no MotionNotify, and waits for its own writer to drain before the next reply (EnterNotify 1, LeaveNotify 1 pass). Later on 2026-09-25: the crossing flags byte was encoded with focus and same-screen swapped (0x01 is focus, 0x02 same-screen), so Xlib read every same-screen crossing as one with the focus (EnterNotify 12, LeaveNotify 14 pass); a client that selects a crossing on another client's window while the pointer is inside has its table register the window's parent and geometry and seed its pointer window from the runtime, so its writer can leave it (LeaveNotify 2 passes); and a map reports VisibilityNotify and Expose on every mapped inferior it makes viewable (VisibilityNotify 3 passes). What remains: a hierarchy change with the pointer over no client toplevel (nothing to route to). ButtonPress 2 (the passive grab from the root down and the NotifyGrab crossings its activation owes) is t220's. VisibilityNotify from occlusion landed 2026-09-25: the window table computes Unobscured, PartiallyObscured or FullyObscured for the windows some client selected VisibilityChange on (a stacking index built once per call; computing every viewable window was quadratic and starved the suite once it held seven hundred), the map reports the computed state, every window request reports what it changed after its own events, InputOnly windows neither obscure nor report (VisibilityNotify 2, 7-9 pass). Read the post-repair rerun in the note for the crossing purposes the warp repair already settled before scoping the seam. @development id:t211 order:211 [details](docs/notes/investigations/1qfs8k41-the-xts-event-section-on-the-host-and-on-xvnc.md)

2026-09-24 Deliver core device events as the protocol routes them: every client that selected the event on the event window or an ancestor is told (done 2026-09-24: ButtonRelease 2-6, KeyPress 2-7, KeyRelease 2-7, MotionNotify 1 pass; and the same evening a press or key is selected by direction and one nobody selected on the path is no event at all, the implicit grab no longer standing in for the owner's own: ButtonPress 4 and 6, ButtonRelease 3, KeyPress 3, KeyRelease 3 pass; the private native key path still selects both directions by KeyPressMask, private_native_key_routing.rs selected_key_at); an event no client selected on the source window propagates to the nearest ancestor that did, stopped by do-not-propagate (done 2026-09-25 for a client's own windows up to the root: the owner's walk no longer stops at its surface window; ButtonPress 7 and ButtonRelease 4 pass; what remains is that the owner's walk and the fan-out to peers decide the propagation separately, so a peer selecting on the toplevel does not stop the owner's own delivery on the root); subwindow names the child of the event window on the way to the source (done 2026-09-25: the writer fills the child from the source's ancestry, for keys from the pointer's; ButtonPress 8-9, ButtonRelease 5-6, KeyPress 5-6, KeyRelease 5-6, MotionNotify 15-16 pass); and motion with a button held is reported to Button<n>Motion selectors (MotionNotify 3-9; done 2026-09-25 for the wheel buttons, which XTEST had dropped as wheel steps and now presses and holds as buttons with their own state bit: MotionNotify 6 and 7 pass, and 3-5, 8-9 already did); MotionNotify 13 is the suite's own (Button1 left pressed by its unresolved purposes 10 and 11, on Xvnc too); a key event now carries the pointer's root and window coordinates (KeyPress 1, KeyRelease 1 done 2026-09-24). The events gate read ButtonPress 5 FAIL once under load ('Expected ButtonPress event, got none') and PASS on the rerun, and ButtonPress 10 and ButtonRelease 7 the same way, each once, while a full test suite or another scenario ran beside the gate: the injected purposes are timing-sensitive under load, and the deterministic answer is this row's routing, not a wider allowance. Later on 2026-09-25: a press activates the passive grab of the first ancestor of the source window searching from the root down, over the ancestry of the window the surface owner's writer last put the pointer in (before, the first matching grab in GrabButton order took it, whatever its window), and the activation crosses the pointer to the grab window with mode NotifyGrab and the release back with NotifyUngrab, the routed event carrying the crossing to the writer (ButtonPress 2 passes). GrabPointer's activation and UngrabPointer owe the same crossings on the request path and do not generate them yet; a peer selecting crossings on the grab path is not told under the grab. Read the post-repair rerun in the note before scoping. @development id:t220 order:220 [details](docs/notes/investigations/1qfs8k41-the-xts-event-section-on-the-host-and-on-xvnc.md)

(B) 2026-09-24 Decide whether the conformance host may honour SetFontPath, so the XTS text scenario can run: XDrawString/16, XDrawText/16 and XDrawImageString/16 (236 purposes) are all UNINITIATED on the host and on Xvnc, because the suite's fontstartup (xts5/src/lib/startup.c) calls XSetFontPath(XT_FONTPATH) and treats any error as fatal, while ADR n520o0bl makes SetFontPath BadAccess. The fonts themselves resolve: a host given xts5/fonts on its path gets past OpenFont('xtfont0') and fails only at SetFontPath. A host-only frontend option, like t189's toplevel placement, plus XT_FONTPATH in check.sh would let the scenario run; the decision is the ADR's scope. The same font startup keeps XTS's XChangeGC, XCreateGC, XGetGCValues and XSetFont out of the gc scenario. +important @planning id:t201 order:201

2026-09-24 Name the x11_wire test that can hang: a full 'cargo test -p sophia-x-authority' on the pane's t199 branch (rebased on 34b78d63) had the x11_wire binary blocked for 22 minutes at under 1% CPU until killed, the quiet output naming no test; a rerun of all 495 tests finished in seconds, as did a second full suite. The likely shape is a routed-socket test waiting on a peer that never answers, the same family as t194's one-offs. Run the suite under a timeout with --test-threads=1 or per-test timing when it recurs, name the test, and give its wait a deadline that fails instead of parking. Seen again 2026-09-25 on xts-events/t229-protocol: a full 'cargo test -p sophia-x-authority --all-features' ran past its 1800-second budget; the rerun printing each binary, and each of the lib, x11_wire and authority binaries alone, passed in seconds, so the binary is still unnamed. And x11_socket::routing_tests::m3_acceptance::b_shared_hold (m3_acceptance_keyboard.rs:296) failed once in a full lib run on xts-events/t211-visibility and passed three times alone, 2026-09-25. Hunted 2026-09-25 through fourteen full iterations of every authority test binary under a deadline with a stack dump (/proc, no gdb): no hang; the day's three real parks were all wire tests reading a record that never came with no socket timeout (two of them new tests, one under a lock-order deadlock the input writer's authority-then-selections order exposed when a dispatcher read the runtime's pointer under the selection lock), named only by libtest's sixty-second notice. Done 2026-09-25: the wire record reader sets its twenty-second default on any socket that has no read timeout, so a wait ends in a panic naming the test and how much of the record arrived; what remains is naming the original full-suite hang if it recurs, now impossible to park. The core probe's set_pointer_mapping (little) case, which only sets a mapping and reads two MappingNotify events, timed out twice on 2026-09-25 (on the t228 hunt's tree and on b6fc3365), passing on every rerun; the probe's own bound names it, and the shape is a MappingNotify broadcast late under load, not a park. @development id:t228 order:228 [details](docs/notes/investigations/1qfs8k41-the-xts-event-section-on-the-host-and-on-xvnc.md)

(B) 2026-09-24 Order a routed event before the reply to a later request from the same client: on the routed frontend a device event is written by the connection's input writer and the reply to the client's next request by its request thread, with nothing ordering the two on the socket, so a client that injects, syncs and then checks its queue (every XTS Xlib11 input purpose, and any client relying on XSync to have flushed events) can read the reply first and find no event; under load the events gate has read ButtonPress 5, ButtonPress 10 and ButtonRelease 7 that way, once each, every one passing when run alone. The private path's ordered delivery (writers/ordered_delivery.rs) already serialises frames per delivery; the routed path needs the same promise: an event enqueued for a client before its request is dispatched is on the wire before that request's reply. Until then the events gate is run alone and a miss of this shape is rerun, not declared. KeymapNotify 1 read FAIL ('No events received' on four of five warps) in an adapter run made beside the full test suites and PASS under the quiet gate on the same host build, 2026-09-25. Done in part 2026-09-25: an injecting connection (FakeInput, and a WarpPointer from a client that may inject) does not read its next request until its own input writer has finished with everything routed to it, through a per-client watermark the registry raises as it queues and the writer as it drains (bounded at one second, the keyboard readiness wait being the writer's own); the ordering test is an_injections_events_precede_the_reply_to_the_next_request. And the protocol side the same day: a request read after an event was queued for the connection has its reply written after that event, through a per-client protocol watermark the registry's sender raises as it queues and the protocol writer as it drains, waited on (bounded at a quarter second) before a request's outputs are written; the mechanism's own test is x11_socket/tests/protocol_watermark.rs, the socket guard an_event_routed_before_a_request_precedes_its_reply. Done 2026-09-25: every connection's request thread also waits, bounded at a quarter second, for its own input writer to drain what was queued before the request was read, the same mark-at-read as the protocol side on the input watermark, so a fanned-out copy or a physical event on a client that did not inject it precedes that client's next reply; the socket guard is a_peers_reply_follows_the_event_fanned_out_before_its_request, which holds the order twenty times over though the race did not fail it reliably before. What remains: two connections' requests are dispatched in parallel, so an event a peer's request queues after this connection read its own is legitimately either side of the reply. @development id:t229 order:229 [details](docs/notes/investigations/1qfs8k41-the-xts-event-section-on-the-host-and-on-xvnc.md)
