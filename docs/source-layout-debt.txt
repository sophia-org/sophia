# Exact known source-layout debt. This is not an exception ledger: every row
# remains a failure in tools/audit_source_layout.sh and must be removed. The
# xtask layout gate prevents a new violation from hiding behind a reduced count.
#
# A size row carries the line count it was admitted at, and that number is a
# ceiling rather than a measurement: the file may shrink freely, and a commit
# that pushes it past the number fails the gate until someone raises it on
# purpose. Without it a ledgered file was never measured again, which is how
# one test file reached forty thousand lines with this gate green throughout.
large crates/sophia-x-authority/src/wire/request.rs 1355
