{
  "/Xproto/pChangeHosts": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pListHosts": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pSetAccessControl": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pGetMotionEvents": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pGetPointerControl": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pChangePointerControl": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pSetPointerMapping": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pQueryKeymap": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pChangeKeyboardControl": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pChangeKeyboardMapping": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pSetModifierMapping": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pGetScreenSaver": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pSetScreenSaver": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pKillClient": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pSetCloseDownMode": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pChangeSaveSet": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pRotateProperties": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pCirculateWindow": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pUnmapSubwindows": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pChangeActivePointerGrab": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pSetFontPath": "t166: the authority answers BadRequest to this core opcode; decide and decode it",
  "/Xproto/pAllocColorCells": "t169: what a TrueColor-only authority owes the colormap requests is undecided; the suite wants BadAlloc for cells and planes on the static visual and BadAccess for read-only cells",
  "/Xproto/pAllocColorPlanes": "t169: what a TrueColor-only authority owes the colormap requests is undecided; the suite wants BadAlloc for cells and planes on the static visual and BadAccess for read-only cells",
  "/Xproto/pCopyColormapAndFree": "t169: what a TrueColor-only authority owes the colormap requests is undecided; the suite wants BadAlloc for cells and planes on the static visual and BadAccess for read-only cells",
  "/Xproto/pFreeColors": "t169: what a TrueColor-only authority owes the colormap requests is undecided; the suite wants BadAlloc for cells and planes on the static visual and BadAccess for read-only cells",
  "/Xproto/pInstallColormap": "t169: what a TrueColor-only authority owes the colormap requests is undecided; the suite wants BadAlloc for cells and planes on the static visual and BadAccess for read-only cells",
  "/Xproto/pUninstallColormap": "t169: what a TrueColor-only authority owes the colormap requests is undecided; the suite wants BadAlloc for cells and planes on the static visual and BadAccess for read-only cells",
  "/Xproto/pListInstalledColormaps": "t169: what a TrueColor-only authority owes the colormap requests is undecided; the suite wants BadAlloc for cells and planes on the static visual and BadAccess for read-only cells",
  "/Xproto/pStoreColors": "t169: what a TrueColor-only authority owes the colormap requests is undecided; the suite wants BadAlloc for cells and planes on the static visual and BadAccess for read-only cells",
  "/Xproto/pStoreNamedColor": "t169: what a TrueColor-only authority owes the colormap requests is undecided; the suite wants BadAlloc for cells and planes on the static visual and BadAccess for read-only cells",
  "/Xproto/pOpenFont": "no server-side fonts by design: XT_FONTPATH is empty because no font opcode is decoded",
  "/Xproto/pCloseFont": "no server-side fonts by design: XT_FONTPATH is empty because no font opcode is decoded",
  "/Xproto/pQueryFont": "no server-side fonts by design: XT_FONTPATH is empty because no font opcode is decoded",
  "/Xproto/pQueryTextExtents": "no server-side fonts by design: XT_FONTPATH is empty because no font opcode is decoded",
  "/Xproto/pPolyText16": "no server-side fonts by design: XT_FONTPATH is empty because no font opcode is decoded",
  "/Xproto/pImageText16": "no server-side fonts by design: XT_FONTPATH is empty because no font opcode is decoded",
  "/Xproto/pCreateGlyphCursor": "no server-side fonts by design: XT_FONTPATH is empty because no font opcode is decoded",
  "/Xproto/pGetFontPath": "no server-side fonts by design: XT_FONTPATH is empty because no font opcode is decoded",
  "/Xproto/pBell": "the suite's own UNTESTED: a four-byte request cannot be made shorter than its minimum",
  "/Xproto/pForceScreenSaver": "the suite's own UNTESTED: a four-byte request cannot be made shorter than its minimum",
  "/Xproto/pGetInputFocus": "the suite's own UNTESTED: a four-byte request cannot be made shorter than its minimum",
  "/Xproto/pGetKeyboardControl": "the suite's own UNTESTED: a four-byte request cannot be made shorter than its minimum",
  "/Xproto/pGetModifierMapping": "the suite's own UNTESTED: a four-byte request cannot be made shorter than its minimum",
  "/Xproto/pGetPointerMapping": "the suite's own UNTESTED: a four-byte request cannot be made shorter than its minimum",
  "/Xproto/pGrabServer": "the suite's own UNTESTED: a four-byte request cannot be made shorter than its minimum",
  "/Xproto/pUngrabServer": "the suite's own UNTESTED: a four-byte request cannot be made shorter than its minimum",
  "/Xproto/pListExtensions": "the suite's own UNTESTED: a four-byte request cannot be made shorter than its minimum",
  "/Xproto/pNoOperation": "the suite's own UNTESTED: a four-byte request cannot be made shorter than its minimum",
  "/Xproto/pCreateWindow#6": "t167: a set unused value-mask bit must be BadValue; accepted silently",
  "/Xproto/pChangeWindowAttributes#4": "t167: a set unused value-mask bit must be BadValue; accepted silently",
  "/Xproto/pConfigureWindow#4": "t167: a set unused value-mask bit must be BadValue; accepted silently",
  "/Xproto/pCreateGC#6": "t167: a set unused value-mask bit must be BadValue; answered BadLength because the extra value changes the length before the mask is read",
  "/Xproto/pSendEvent#1": "t168: an event delivered by SendEvent must carry bit 7 of its type set",
  "/Xproto/pAllocColor#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pAllocColorCells#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pAllocColorPlanes#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pAllocNamedColor#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pAllowEvents#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pBell#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pChangeActivePointerGrab#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pChangeGC#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pChangeKeyboardControl#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pChangeKeyboardMapping#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pChangeProperty#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pChangeSaveSet#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pChangeWindowAttributes#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pCirculateWindow#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pClearArea#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pConfigureWindow#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pConvertSelection#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pCopyArea#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pCopyColormapAndFree#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pCopyGC#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pCopyPlane#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pCreateColormap#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pCreateCursor#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pCreateGC#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pCreatePixmap#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pCreateWindow#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pDeleteProperty#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pDestroySubwindows#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pDestroyWindow#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pFillPoly#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pForceScreenSaver#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pFreeColormap#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pFreeColors#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pFreeCursor#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pFreeGC#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pFreePixmap#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetAtomName#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetFontPath#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetGeometry#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetImage#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetInputFocus#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetKeyboardControl#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetKeyboardMapping#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetModifierMapping#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetMotionEvents#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetPointerControl#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetPointerMapping#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetProperty#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetScreenSaver#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetSelectionOwner#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGetWindowAttributes#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGrabButton#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGrabKey#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGrabKeyboard#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGrabPointer#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pGrabServer#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pImageText8#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pInstallColormap#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pInternAtom#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pKillClient#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pListExtensions#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pListFonts#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pListFontsWithInfo#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pListHosts#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pListInstalledColormaps#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pListProperties#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pLookupColor#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pMapSubwindows#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pMapWindow#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pNoOperation#4": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pPolyArc#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pPolyFillArc#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pPolyFillRectangle#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pPolyLine#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pPolyPoint#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pPolyRectangle#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pPolySegment#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pPolyText8#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pPutImage#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pQueryBestSize#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pQueryColors#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pQueryExtension#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pQueryKeymap#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pQueryPointer#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pQueryTree#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pRecolorCursor#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pReparentWindow#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pRotateProperties#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pSendEvent#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pSetAccessControl#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pSetClipRectangles#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pSetCloseDownMode#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pSetDashes#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pSetFontPath#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pSetInputFocus#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pSetModifierMapping#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pSetPointerMapping#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pSetSelectionOwner#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pStoreColors#4": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pStoreNamedColor#4": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pTranslateCoords#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pUngrabButton#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pUngrabKey#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pUngrabKeyboard#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pUngrabPointer#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pUngrabServer#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pUninstallColormap#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pUnmapSubwindows#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pUnmapWindow#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength",
  "/Xproto/pWarpPointer#3": "t174: BIG-REQUESTS is advertised but an extended-length request is not framed: the zero-length header is read as a four-byte request and the 262 KB body as some 65 000 requests, each answered, where the suite wants one BadLength"
}
