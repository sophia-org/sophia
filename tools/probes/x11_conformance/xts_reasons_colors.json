{
  "/Xlib10/XUninstallColormap#1": "the suite's own omission: 'There is no known reliable test method for this assertion'",
  "/Xlib10/XUninstallColormap#3": "the suite's own omission: 'There is no known portable test method for this assertion'",
  "/Xlib10/XUninstallColormap#4": "the suite's own omission: 'There is no known reliable test method for this assertion'",
  "/Xlib7/XAllocColor#5": "the suite's own omission: 'There is no known portable test method for this assertion'",
  "/Xlib7/XAllocColorCells#1": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XAllocColorCells#2": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XAllocColorCells#3": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XAllocColorCells#4": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XAllocColorCells#5": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XAllocColorCells#6": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XAllocColorPlanes#1": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XAllocColorPlanes#2": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XAllocColorPlanes#3": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XAllocColorPlanes#4": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XAllocColorPlanes#6": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XCopyColormapAndFree#1": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XCopyColormapAndFree#2": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XCopyColormapAndFree#3": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XCopyColormapAndFree#4": "the suite's own omission: 'There is no known portable test method for this assertion'",
  "/Xlib7/XCreateColormap#13": "the suite's own omission: 'There is no known portable test method for this assertion'",
  "/Xlib7/XCreateColormap#2": "the suite's own omission: 'The statement in the X11 specification is not specific enough to write a test'",
  "/Xlib7/XCreateColormap#3": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XCreateColormap#4": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XCreateColormap#5": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XCreateColormap#6": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XCreateColormap#7": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XCreateColormap#9": "the suite's own omission: 'There is no known reliable test method for this assertion'",
  "/Xlib7/XFreeColors#1": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XFreeColors#2": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XFreeColors#3": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XFreeColors#4": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XFreeColors#5": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XFreeColors#6": "the suite expects BadValue when freeing a duplicate allocation and for all-one pixels on ARGB; dix instead reports BadAccess for missing component references and permits ARGB alpha bits; Sophia now agrees with dix (t212)",
  "/Xlib7/XFreeColors#9": "the suite expects BadValue for its mixed invalid-pixel list, but dix reports BadAccess when an unallocated component is encountered after an invalid pixel; Sophia now follows the same per-channel error order (t212)",
  "/Xlib7/XParseColor#1": "the suite's own omission: 'There is no known portable test method for this assertion'",
  "/Xlib7/XQueryColor#3": "Xorg's own server (Xvnc, 2026-09-24) gives the same FAIL: REPORT: Got Success, Expecting BadValue",
  "/Xlib7/XQueryColors#2": "Xorg's own server (Xvnc, 2026-09-24) gives the same FAIL: REPORT: Got Success, Expecting BadColor",
  "/Xlib7/XQueryColors#3": "Xorg's own server (Xvnc, 2026-09-24) gives the same FAIL: REPORT: Got Success, Expecting BadValue",
  "/Xlib7/XStoreColor#1": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreColor#2": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreColor#3": "the suite's own omission: 'There is no known portable test method for this assertion'",
  "/Xlib7/XStoreColor#4": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreColor#7": "the suite's own omission: 'There is no known reliable test method for this assertion'",
  "/Xlib7/XStoreColor#8": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreColors#1": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreColors#2": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreColors#3": "the suite's own omission: 'There is no known portable test method for this assertion'",
  "/Xlib7/XStoreColors#4": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreColors#5": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreColors#8": "the suite's own omission: 'There is no known reliable test method for this assertion'",
  "/Xlib7/XStoreColors#9": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreNamedColor#1": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreNamedColor#2": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreNamedColor#3": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreNamedColor#4": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreNamedColor#5": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreNamedColor#7": "needs a writable or grey visual class (PseudoColor, GrayScale or DirectColor); Sophia's screen serves TrueColor visuals only, so the suite skips it",
  "/Xlib7/XStoreNamedColor#8": "the suite's own omission: 'There is no known reliable test method for this assertion'",
  "/Xlib7/XStoreNamedColor#9": "the suite's own omission: 'There is no known reliable test method for this assertion'"
}
